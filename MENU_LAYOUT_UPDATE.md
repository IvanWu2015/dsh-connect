# 菜单布局优化

## 🎯 问题

用户反馈："菜单全部改为两行一列，其中工作区、对话列表，一行一列。其他的你再检查一下，不要有那种只显示了一小部分的情况。"

**原问题**：
- 菜单使用 **3列布局**（每行3个按钮），导致某些长文本被截断或显示不完整
- 工作区和对话列表项较多时，3列布局显得拥挤
- 没有针对不同菜单类型优化布局

## ✅ 解决方案

已重新设计菜单布局系统：

### 新布局规则

1. **默认布局**：**2列**（每行2个按钮）
   - 适用于大多数菜单（设置、任务、系统等）
   - 提供更好的可读性和点击体验

2. **全宽布局**：**1列**（每行1个按钮）
   - 工作区列表（workspace menu）
   - 对话列表（chat menu）
   - 根菜单中的工作区和会话 section
   - 适合显示较长的路径和标题

### 技术实现

#### 1. 扩展 `ChoicePrompt` 接口

```typescript
export interface ChoicePrompt {
  // ... existing fields
  /** Number of columns per row for button grid (default: 2). Use 1 for full-width items. */
  readonly columnsPerRow?: number;
}
```

#### 2. 扩展 `ChoiceSection` 接口

```typescript
export interface ChoiceSection {
  // ... existing fields
  /** Number of columns per row for this section (overrides prompt default). */
  readonly columnsPerRow?: number;
}
```

#### 3. 更新 `buildButtonGrid` 函数

```typescript
export function buildButtonGrid(
  options: readonly ChoiceOption[], 
  columnsPerRow: number = 2  // 可配置列数
): unknown[] {
  // 根据 columnsPerRow 动态生成网格
  for (let i = 0; i < padded.length; i += columnsPerRow) {
    // ...
  }
}
```

#### 4. 智能布局选择

在 `openMenu` 中根据菜单类型自动选择列数：

```typescript
// Workspace and chat lists use 1 column (full width), others use 2 columns
const columnsPerRow = (menuId === "workspace" || menuId === "chat") ? 1 : 2;
```

在根菜单 sections 中为不同分组设置不同列数：

```typescript
private rootMenuSections() {
  return [
    { title: "📁 工作区", ids: ["workspace"], columnsPerRow: 1 },
    { title: "💬 会话", ids: ["chat", "history"], columnsPerRow: 1 },
    { title: "📋 任务", ids: ["task", "goals", "schedule"], columnsPerRow: 2 },
    { title: "🛠️ 系统", ids: ["status", "plugins", "compact", "settings"], columnsPerRow: 2 },
  ];
}
```

## 📋 布局效果对比

### Before（之前 - 3列布局）

**根菜单**：
```
┌─────────────┬─────────────┬─────────────┐
│ 📁 切换工作 │ 💬 切换对话 │ 📊 查看状态 │
├─────────────┼─────────────┼─────────────┤
│ 📋 查看任务 │ 🗒️ 历史消息 │ 🎯 目标     │
├─────────────┼─────────────┼─────────────┤
│ ⏰ 定时提醒 │ 🗜️ 压缩上下 │ 🔌 查看插件 │
├─────────────┼─────────────┼─────────────┤
│ ⚙️ 设置     │ ❌ 退出     │ 🔙 返回     │
└─────────────┴─────────────┴─────────────┘
```

**问题**：
- ❌ 文字被截断："🗜️ 压缩上下" 应该是 "🗜️ 压缩上下文"
- ❌ 按钮太窄，不易点击
- ❌ 视觉拥挤

### After（现在 - 2列/1列混合布局）

**根菜单**：
```
┌─────────────────────────────────────────┐
│ **📁 工作区**                            │
├─────────────────────────────────────────┤
│ 📁 切换工作目录                          │
├─────────────────────────────────────────┤
│ **💬 会话**                              │
├─────────────────────────────────────────┤
│ 💬 切换对话                              │
├─────────────────────────────────────────┤
│ 🗒️ 历史消息                              │
├─────────────────────────────────────────┤
│ **📋 任务**                              │
├──────────────────────┬──────────────────┤
│ 📋 查看任务          │ 🎯 目标          │
├──────────────────────┼──────────────────┤
│ ⏰ 定时提醒          │ 🗜️ 压缩上下文    │
├──────────────────────┼──────────────────┤
│ **🛠️ 系统**                              │
├──────────────────────┼──────────────────┤
│ 📊 查看状态          │ 🔌 查看插件      │
├──────────────────────┼──────────────────┤
│ ⚙️ 设置              │ ❌ 退出          │
└──────────────────────┴──────────────────┘
```

**优势**：
- ✅ 完整显示所有文字
- ✅ 按钮更宽，易于点击
- ✅ 清晰的视觉分组
- ✅ 工作区和会话使用全宽，便于阅读长标题

**工作区菜单（1列）**：
```
┌─────────────────────────────────────────┐
│ 切换工作目录                             │
├─────────────────────────────────────────┤
│ ● 当前目录                               │
├─────────────────────────────────────────┤
│ ○ 项目A  (D:\projects\project-a)        │
├─────────────────────────────────────────┤
│ ○ 项目B  (D:\projects\project-b)        │
├─────────────────────────────────────────┤
│ ❌ 退出                                  │
└─────────────────────────────────────────┘
```

**对话菜单（1列）**：
```
┌─────────────────────────────────────────┐
│ 切换对话                                 │
├─────────────────────────────────────────┤
│ ● 帮我写一个Python脚本 🌐                │
├─────────────────────────────────────────┤
│ ○ 项目需求讨论 🌐                        │
├─────────────────────────────────────────┤
│ ○ 代码审查                               │
├─────────────────────────────────────────┤
│ ➕ 新建对话                              │
├─────────────────────────────────────────┤
│ ❌ 退出                                  │
└─────────────────────────────────────────┘
```

## 🔧 修改的文件

### 1. `packages/connect/src/types.ts`
- 添加 `ChoicePrompt.columnsPerRow` 字段
- 添加 `ChoiceSection.columnsPerRow` 字段

### 2. `packages/connect-feishu/src/adapter.ts`
- 修改 `buildButtonGrid()` 支持可配置列数
- 修改 `buildChoiceElements()` 支持 section 级别列数
- 修改 `promptChoice()` 读取并使用 `columnsPerRow`

### 3. `packages/connect/src/runner.ts`
- 修改 `openMenu()` 根据菜单类型设置列数
- 修改 `rootMenuSections()` 为不同 section 设置列数

## 🎨 设计原则

### 为什么选择 2列作为默认？

1. **平衡性**：比3列更宽松，比1列更紧凑
2. **可读性**：按钮有足够的宽度显示完整文字
3. **触控友好**：按钮大小适合手指点击
4. **视觉层次**：配合 section 标题形成清晰的结构

### 为什么工作区和对话用1列？

1. **内容较长**：路径和标题通常较长，需要更多空间
2. **重要性**：这些是核心导航项，应该突出显示
3. **一致性**：与文件浏览器等常见UI模式一致
4. **避免截断**：确保完整显示所有信息

## 📊 验证方法

### 手动测试

1. 执行 `/menu` 打开根菜单
   - 确认工作区和会话 section 是全宽（1列）
   - 确认任务和系统 section 是2列
   - 确认所有文字完整显示

2. 进入工作区菜单
   - 确认所有工作区路径完整显示
   - 确认是单列布局

3. 进入对话菜单
   - 确认所有会话标题完整显示
   - 确认 🌐 标识正常显示
   - 确认是单列布局

4. 进入设置菜单
   - 确认是2列布局
   - 确认所有选项完整显示

### 检查要点

- ✅ 没有文字被截断
- ✅ 按钮大小均匀
- ✅ 布局整齐对齐
- ✅ 不同菜单类型使用正确的列数
- ✅ Section 标题清晰可见

## 💡 未来增强建议

1. **响应式布局**：根据屏幕宽度自动调整列数
2. **用户偏好**：允许用户自定义默认列数
3. **动态调整**：根据选项数量自动选择最佳列数
4. **预览模式**：在发送前预览菜单布局效果

## ✨ 总结

新的菜单布局系统提供：
- ✅ **更好的可读性**：完整显示所有文字
- ✅ **更好的可用性**：更大的点击区域
- ✅ **更好的视觉层次**：清晰的分组和结构
- ✅ **灵活的配置**：支持菜单级别和 section 级别的列数控制

用户可以更轻松地浏览和操作菜单了！🎉
